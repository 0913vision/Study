dp[i][j]

i = i번째 사건
j = 해결한 경찰차번호(0, 1)

move(j) = j번째 경찰차가 i번째 사건 위치로 움직인 거리

dp[i][j] = min(dp[i-1][0]+move(i, 0, j), dp[i-1][1]+move(i, 1, j))
2000 (최대배열크기)

car[i][j][k][t]

i = 사건번호
j = 해결한 경찰차번호 (0 or 1)
k = 차량번호
t = 차량좌표(t1, t2)

1000*2*2*2 = 8000 (최대배열크기)

따라서 최대배열 크기는 10000 -> 40000 바이트 ~= 39kb

즉 move(event, dispatch, target) = dist(i번째 사건 위치, car[i-1][dispatch][target])
dist(a1, a2, b1, b2) = |a1-a2|+|b1-b2|
